{% if attributes -%}
<div{{ attributes }}>
    {% endif %}
    {% if title %}
        <h3>{{ title }}</h3>
    {% endif %}

    {% for row in rows %}
        <div{{ row.attributes.addClass(grid_classes) }}>{{ row.content }}</div>
    {% endfor %}


    {% if attributes -%}
</div>
{% endif %}